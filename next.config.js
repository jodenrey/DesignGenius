/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upcdn.io",
      "replicate.delivery",
      "designgenuis-images.s3.amazonaws.com",
      " images.unsplash.com"
    ]
  }
};

module.exports = nextConfig;
